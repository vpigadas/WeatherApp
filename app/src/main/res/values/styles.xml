<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/background</item>
        <item name="android:colorForeground">@color/foreground</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColorPrimary">@color/colorTxtMain</item>
        <item name="android:textColorSecondary">@color/colorTxtMain</item>
        <item name="android:textColorTertiary">@color/colorTxtMain</item>

        <item name="android:textColorLink">@color/colorTxtHint</item>
        <item name="android:textColorHint">@color/colorTxtHint</item>

        <item name="android:textColorPrimaryInverse">@color/colorTxtHint</item>
        <item name="android:textColorSecondaryInverse">@color/colorTxtHint</item>
        <item name="android:textColorTertiaryInverse">@color/colorTxtHint</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
